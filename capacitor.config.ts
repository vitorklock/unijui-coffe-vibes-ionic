import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'unijui-coffe-vibes-ionic-2',
  webDir: 'dist'
};

export default config;
